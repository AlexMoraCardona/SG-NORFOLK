<tr>
    <td><%= customer.customer_name %></td>
    <td><%= customer.name %></td>
    <td><%= customer.ruc %></td>
    <td><%= link_to t('common.show'), customer_path(customer.id), class: 'btn btn-sm btn-primary'  %></td>
    <td><%= link_to t('common.edit'), edit_customer_path(customer.id), class: 'btn btn-sm btn-primary'  %></td>
</tr>